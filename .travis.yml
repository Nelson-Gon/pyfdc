# CI Travis
sudo: false

language: python

python:
    - 3.5
    - 3.6
    - 3.7
    - 3.8

before_install:
    - pip install -U pip
    - pip install -r requirements.txt

script:
    - python python-fdc/setup.py install || python3 python-fdc/setup.py install
